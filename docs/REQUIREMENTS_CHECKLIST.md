# V1 requirements and acceptance evidence

This checklist preserves the full scope of `REQUIREMENT.md` and `DESIGN.md`.
Statuses are **PASS**, **PARTIAL**, **NOT IMPLEMENTED**, or **BLOCKED**.
PASS requires evidence for the entire row; tests for one part do not prove the rest.
Rows describe the reviewed Gate A–F implementation and its validation evidence. Remaining
PARTIAL rows identify native interactions the available environment could not fully exercise.

| ID | Requirement | Gate | Status | Evidence or missing work |
| --- | --- | --- | --- | --- |
| R01 | Native Swift 6, SwiftUI, macOS 26+ application; only necessary AppKit; offline core | A–F | PASS | Final native Xcode app/local Swift package builds in Debug and universal Release; independent review confirms no third-party or network dependency. |
| R02 | `NSPasteboard.general`, changeCount, 300–500 ms polling, cheap unchanged ticks, unsupported types safe | A | PASS | Production general-board wiring, 400 ms monitor, unit tests and private-board native timer smoke. Real system consent dialog untested. |
| R03 | In-memory clipboard history, plain text and URL capture | A | PASS | Native app smoke shows text/URL rows; capture tests use real named boards. |
| R04 | Required history fields: identity/hash/type/text/source/name/bundle/times/pin/size/metadata/payload reference | A–E | PASS | Immutable Sendable/Codable models contain these fields; schema v2 blob storage is verified in R24. |
| R05 | Consecutive and recent duplicate reuse, preserve pin/identity, update timestamps | A–B | PASS | In-memory and SQLite dedup/pin/identity/timestamp tests pass; native duplicate capture after restart reused one row. |
| R06 | Internal clipboard writes do not create history entries; later external copies still work | A–D | PASS | Core race/suppression tests pass; native copy and trusted paste preserve the two synthetic content/FTS rows without duplicates. |
| R07 | Local SQLite persistence and migrations; restart recovery | B | PASS | Actor repository, transactional schema creation, future-version rejection/migration rollback tests, native quit/relaunch recovery pass. |
| R08 | Transactional writes and consistent local search index | B | PASS | SQLite transactions and FTS5 triggers; update/delete/retention/search and failed-write rollback tests pass. |
| R09 | Retain at most 1,000 unpinned items / 30 days by default; pins never expire normally | B | PASS | Defaults defined; injected-policy tests verify exact age boundary, newest-count retention, unpin pruning and pin preservation. |
| R10 | Interactive search of text, URLs/domains, filenames, source app and metadata | B–F | PASS | FTS searches text, URLs/domains, ordered filenames, source names/bundle IDs and type metadata. Native text/rich/file filtering and 1,000-row search pass; measurements are in PERFORMANCE.md. |
| R11 | All/Text/Code/Links/Images/Files/Pinned filters | B–E | PASS | All seven repository filters pass tests. Native text/code/link/image/file/pinned checks and final pinned-image restart pass. |
| R12 | Global `⌘⇧V` toggles floating panel | C | PASS | Metadata trace verified Carbon delivery; user confirmed normal-build shortcut opening/search and shortcut close after diagnostic removal. |
| R13 | Open remembers previous app, chooses active display, focuses search, selects newest result | C | PARTIAL | Native cooperative reopening focuses search; Escape/copy restore the actual prior foreground app and editor typing. Single-display placement and newest-row reopening pass. User confirmed physical shortcut opening with search focused. Normal-build shortcut/search/close confirmed by user; multiple-display checks remain. |
| R14 | Copy-only always restores old content without Accessibility trust | C–D | PASS | Native exact private-board restoration passes before and after the AX grant. CmdReturn with trust enabled leaves the editor and its paste count unchanged. Hydration/recency/suppression tests pass. General clipboard consent is separately tracked in R02. |
| R15 | Auto-paste closes panel, restores previous app, confirms focus, posts normal paste when trusted | D | PASS | Reviewed coordinator, permission/target/focus tests and native trusted Return pass: the synthetic editor received the exact selected fixture through its normal paste handler. Automation limitations are recorded in STATUS.md. |
| R16 | Failed auto-paste leaves content copied for manual `⌘V`; no synthetic paste without trust | D | PASS | Permission/revocation, target/focus, cancellation and receipt-replacement tests pass. Native denied-destination fallback copies and closes. A newer external clipboard write is respected rather than overwritten. |
| R17 | No clipboard uploads, networking/telemetry containing data, payload logs, or real secrets in fixtures | A–F | PASS | Final independent core/app review found no networking/payload logging; synthetic/private fixtures only and native app logs empty. Restore uses currentHostOnly. Debug flags are absent from Release. |
| R18 | Exclude applications by bundle ID before capture/persistence where possible | E | PASS | Exclusions persist by bundle ID; all item sources are preflighted before payload reads. Native excluded synthetic source omitted; fail-closed settings and in-flight cancellation tests pass. |
| R19 | Pause/resume recording; ignore appropriate concealed/transient content | A–E | PASS | Pause/resume/sleep policy generations and baseline transitions pass tests. Native paused copy is omitted without catch-up; resumed copy appears; concealed second item rejects the whole capture. |
| R20 | Delete one item; clear unpinned/all history with confirmation | E | PASS | Keyboard and preview single-delete confirmation/cancel pass natively. Confirmed deletion removes its result; clear-unpinned preserves the pin/blob, clear-all leaves content/FTS/blob counts zero. |
| R21 | Menu-bar Open Clipboard, Pause/Resume, Clear History, Settings, Quit | E | PARTIAL | NSStatusItem exposes all five required actions and native menu structure was observed. Settings/pause/clear/quit behavior passes through App/UI paths; direct status-menu invocation could not be reliably targeted by automation. |
| R22 | Native ServiceManagement launch at login and useful operation without permanent Dock icon | E | PARTIAL | Native registration reports Enabled and unregister reports Disabled; service tests and menu-hidden Dock fallback pass. A subsequent system login was not performed in this interactive environment. |
| R23 | Images, copied files/folders, practical rich text and faithful multiple representations | E | PASS | Ordered native multi-item capture and restoration preserve supported text/URL/RTF/PNG/TIFF/file representations. Native exact-byte rich text, small/large image, two-file restore and duplicate suppression pass. |
| R24 | Large payloads in Application Support, lazy row loading, deletion/retention/orphan cleanup | E | PASS | Schema v2 externalizes large payloads; metadata-only lists and selected hydration, v1 migration, pin/retention/delete/orphan cleanup, corruption and transactional rollback tests pass. Native clear-all removes the external image blob. |
| R25 | Source-app information presented usefully | E–F | PASS | Rows and previews show searchable source name/bundle ID, type and copied time. Static localized timestamps avoid idle updates. AX row labels also include this metadata. |
| R26 | Full keyboard operation, visible focus and VoiceOver labels | C–F | PARTIAL | Native keyboard/actions and physical shortcut checks pass; rows expose labeled selected AX elements and working Pin/Copy actions, and recorder focus/arming is visible. VoiceOver would not stay enabled, so spoken announcements and cursor/rotor navigation remain unverified. |
| R27 | Reduce Motion, Reduce Transparency, Increase Contrast and Light/Dark support | F | PASS | Native Light/Dark, opaque Reduce Transparency fallback, stronger Increase Contrast selection and Reduce Motion search checks pass. Static presentation avoids decorative motion. Original system preferences were restored and read back. |
| R28 | Accessibility permission requested only for needed behavior; app useful without it | D | PASS | Opening/copy-only never prompt; paste intent/Enable can request trust. Native copy works before grant; user-authorized Settings grant reads back On and permits a verified native insertion. |
| R29 | Negligible idle work, low text-history memory, no heavy synchronous main-actor DB/image work | A–F | PASS | Final 1,000-row Debug idle measured 0.083% of one core open and 0.050% closed, with 112–115 MiB RSS. Unchanged-poll probe made zero payload reads; metadata rows, repository actor and bounded image actor avoid heavy UI work. See PERFORMANCE.md. |
| R30 | Fast panel opening and interactive search; measure hot paths before added complexity | B–F | PASS | Native open/search/preview observations pass with 1,000 rows; timings include automation overhead and do not claim physical-key latency. Optimized metadata/FTS/hydration/hash/blob measurements are recorded in PERFORMANCE.md. |
| R31 | Multiple displays and sleep/wake reliability | E | PARTIAL | Visible-panel display recovery and geometry tests cover negative coordinates, small/disconnected display layouts and hidden-panel behavior. Synchronous sleep/wake and no-catch-up tests pass. Only one physical display is available; actual unplug and sleep/wake were not exercised. |
| R32 | Build/run from Xcode without paid Apple Developer membership | A–F | PASS | Clean Debug and universal Release builds use local ad-hoc signing without a team; strict signatures and isolated native Debug launch pass. |
| R33 | Clean Release `.app` for trusted testers with instructions; `.dmg` optional | F | PASS | Fresh Release app and ZIP contain arm64/x86_64, pass original/extracted strict signature and bundle checks, and omit Debug flags. Tester guide explains installation and permissions. Ad-hoc package is not notarized; DMG is unnecessary for this scope. |
| R34 | Neutral naming, no unapproved public release/tags/credentials | A–F | PASS | ClipboardManager naming retained. Only accepted source/docs/scripts are part of the milestone; ignored artifacts stay local. No release, tag or credential change is made. |
| D01 | 760×520 compact panel slightly above center; useful 1040×640 expanded preview | C | PASS | Geometry tests and native compact/expanded preview inspection pass. Actual compact window bounds verify size and placement about 32 pt above visible center on the connected display. Multiple displays remain R31. |
| D02 | Search/filter/history/footer compact layout with readable rows, source/time/type/pin/sequence | C–F | PASS | Final compact/expanded native inspection passes, including 1,000-row list, source/time/type/pin/sequence metadata and individually labeled AX rows. Viewport numbering follows scrolling. |
| D03 | Native glass limited to chrome/controls/footer/popovers; legible text/code/image content | F | PASS | Native GlassEffectContainer/glassEffect chrome, search/filter/footer/settings; history and preview use opaque semantic surfaces. Light/Dark and transparency/contrast checks pass. |
| D04 | Continuous rounded shapes, consistent spacing, SF Symbols, system/monospaced typography, semantic colors | C–F | PASS | Final independent UI review and native appearance inspection verify these conventions, including the continuous 26-point panel shape and visible recorder focus. |
| D05 | Up/Down select; Return paste; `⌘Return` copy; Space/Right preview; Escape close and return focus | C–D | PASS | Native selection/preview/copy/focus checks pass. Trusted Return inserts the selected synthetic text; CmdReturn copies without insertion. Routing and cancellation tests pass. |
| D06 | `⌘P` pin; `⌘Delete` delete; `⌘K` clear search; `⌘1`–`⌘9` paste visible row | C–E | PASS | CmdP pin and CmdDelete confirmed deletion pass natively. CmdK and viewport-numbered routing/copy were verified in C; numbered paste intent and visible ordering pass tests. |
| D07 | Pointer interaction and explicit Copy/Pin/Delete in preview | C–E | PASS | Pointer select followed by Right previews the selected image, printable input returns to search, and Copy/Pin/Delete controls work. Single and double-click behavior has model/native coverage; preview Delete confirms. |
| D08 | Short subtle motion without continuous decoration; respects Reduce Motion | F | PASS | Accepted static presentation: no decorative animation and panel resize uses animate:false. This respects Reduce Motion; DESIGN.md's fade/scale is recommended rather than mandatory. |
| D09 | General settings: shortcut, launch at login, menu-bar visibility, retention | E | PASS | General settings expose explicit accessible shortcut recording, native login status, menu-bar visibility and count retention. Native custom shortcut persists with matching panel hint and default is restored; focused unarmed recorder permits Quit. |
| D10 | Privacy settings: pause, exclusions, transient handling, clear; Permissions: AX status/explanation | D–E | PASS | Privacy settings expose persisted pause/exclusions and mandatory marker handling, plus confirmed clear. Permissions shows current Accessibility and its purpose; native existing trust reads Enabled. |
| G01 | Every gate independently reviewed, built/tested, accepted, committed and pushed | A–F | PASS | A–F passed independent review, tests, clean signed builds and scoped native checks. Milestone commits and the checked main remote are the publication receipts. |
| G02 | Final independent architecture/privacy/Swift/performance/UI/test/packaging review, findings fixed | F | PASS | Fresh UI and Core/final reviews have no remaining medium/high issue after timestamp and fixture-guard fixes. Final 145-test suite, clean builds and package checks pass. |

Native checks that require unavailable hardware, permissions, or interaction must be recorded honestly
as unverified with the specific reason. They cannot be replaced by unrelated passing unit tests.
